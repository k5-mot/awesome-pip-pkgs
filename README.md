# awesome-pip-pkgs

```bash
pip3 download -d lib \
    python-docx \
    "docling-mcp[local]" \
    docling \
    Pillow \
    "markitdown[pptx,docx,xlsx,xls,pdf]" \
    markitdown-ocr \
    markitdown-mcp
```
